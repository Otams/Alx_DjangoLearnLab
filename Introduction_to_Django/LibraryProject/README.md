# 📚 LibraryProject

This is a Django project created as part of a learning exercise.

### Steps Completed:
1. Installed Django using `pip install django`.
2. Created the project using `django-admin startproject LibraryProject`.
3. Verified the project structure and started the development server with:
   ```bash
   python manage.py runserver
